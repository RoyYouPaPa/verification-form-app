# 更新紀錄 Changelog

本專案所有重要變更皆記錄於此。格式參考 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.0.0/)，版本遵循語意化版本。

## [1.1.2] - 2026-09-06

### 變更
- 檔名中段改由各公司設定的 `fileNameFieldKey` 明確指定（鵬曜=工程名稱、澄遠=驗收項目），不再依賴欄位 fallback 順序。

## [1.1.1] - 2026-09-06

### 變更
- 照片壓縮目標大小上限由 ≈1MB 下修為 **≈500KB**。

## [1.1.0] - 2026-09-06

### 新增
- 照片上傳時於瀏覽器端**自動壓縮**：優先保留尺寸、調降 JPEG 品質，必要時才微縮，讓每張控制在約 1MB 以內；並套用 EXIF 方向避免手機照片轉向。縮圖下方顯示壓縮後大小。

### 變更
- PDF 檔名中段的「驗收單」改為輸入的**工程名稱**（澄遠改用「驗收項目」，未填則退回「驗收單」）。

## [1.0.2] - 2026-09-06

### 變更
- 移除 repo 內的 `SPEC.md`（內部規格文件，非程式必需）。

## [1.0.1] - 2026-09-06

### 新增
- 頁尾顯示版本號碼（版本來源為 `package.json`，build 時注入）。
- 新增本 CHANGELOG。

### 變更
- README 移除公司名稱、統編、聯絡方式與印章現況等描述，改為通用說明。
- 程式碼註解改以版型（`boxed` / `plain`）描述，不再點名特定公司。
- 移除登入頁「此密碼僅用於避免公開瀏覽」提示文字。

## [1.0.0] - 2026-09-05

### 新增
- 初版：選公司別、填欄位、上傳多張照片，一鍵下載 PDF。
- 兩間公司各自獨立版型（`boxed`：客戶資訊加框；`plain`：無框純文字），忠於各自原稿。
- 每頁 4×4 格、15 張照片 + 右下印章；照片區為單一粗外框、內部不畫格線。
- 照片超過 15 張自動換頁、重複表頭、頁數遞增。
- 輸入表單欄位左右逐列對齊（右欄較少時留空不歪斜）。
- pdf-lib 內嵌照片與中文字型（Noto Sans TC）。
- 前端密碼保護、儲存抽象層 + env 開關（預設 `none`，不儲存）。
- 缺少印章檔時以 magic bytes 驗證並顯示占位框，不會出錯。
- GitHub Pages 自動部署 workflow。
